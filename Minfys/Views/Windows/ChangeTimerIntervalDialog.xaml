<Window x:Class="Minfys.Views.Windows.ChangeTimerIntervalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:windows="clr-namespace:Minfys.ViewModels.Windows"
        d:DataContext="{d:DesignInstance Type=windows:ChangeTimerIntervalDialogViewModel, IsDesignTimeCreatable=True}"
        mc:Ignorable="d"
        Title="ChangeTimerIntervalWindow"
        Width="250"
        Height="110"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FocusManager.FocusedElement="{Binding ElementName=NewTimerIntervalMaskedTextBox}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0"
                    Grid.Row="0"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal">
            <TextBlock Text="Enter new value:"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Margin="5"
                       FontSize="20" />
            <xctk:MaskedTextBox Name="NewTimerIntervalMaskedTextBox"
                                Mask="00:00:00"
                                FontSize="20"
                                Margin="5, 5, 5, 0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                PromptChar="0" />
        </StackPanel>
        <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <Button Name="AcceptChangeButton"
                    Content="Accept" Margin="5" FontSize="15"
                    Command="{Binding AcceptChangeCommand}"
                    CommandParameter="{Binding Text, ElementName=NewTimerIntervalMaskedTextBox}" />
            <Button Name="CancelChangeButton"
                    Content="Cancel" Margin="5" FontSize="15"
                    Command="{Binding CancelChangeCommand}" />
        </StackPanel>

    </Grid>
</Window>