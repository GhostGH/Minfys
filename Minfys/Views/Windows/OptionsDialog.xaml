<Window x:Class="Minfys.Views.Windows.OptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:windows="clr-namespace:Minfys.ViewModels.Windows"
        xmlns:local="clr-namespace:Minfys.Models"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=windows:OptionsDialogViewModel, IsDesignTimeCreatable=True}"
        Title="Settings" Height="450" Width="300"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource MaterialDesignWindow}">
    <Border
        BorderBrush="#5c5c5c"
        BorderThickness="1.5">

        <Grid Margin="10, 10, 10, 10" ShowGridLines="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="70"></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Application mode" VerticalAlignment="Center" FontSize="14"></TextBlock>
                <ComboBox Grid.Column="1" VerticalAlignment="Center"
                          ItemsSource="{Binding Source={x:Static local:TimerModesOptions.AvailableModes}}"
                          SelectedItem="{Binding TimerMode}" HorizontalAlignment="Right">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <b:InvokeCommandAction
                                Command="{Binding TimerModeOptionChangedCommand}"
                                CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ComboBox>
            </Grid>

            <Grid Grid.Row="1" ShowGridLines="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Enable loop" VerticalAlignment="Center" FontSize="14">
                </TextBlock>
                <CheckBox Grid.Column="1"
                          VerticalAlignment="Center"
                          FontSize="15"
                          Margin="5"
                          IsEnabled="{Binding IsLoopOptionAvailable}"
                          IsChecked="{Binding LoopEnabled}"
                          Command="{Binding LoopOptionChangedCommand}" />
                <materialDesign:PackIcon Grid.Column="2" VerticalAlignment="Center"
                                         Kind="InformationOutline"
                                         Width="18" Height="18"
                                         Foreground="MediumPurple"
                                         Background="Transparent"
                                         ToolTip="Loop cannot be enabled if Single mode is enabled." />
            </Grid>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock HorizontalAlignment="Left"
                           Text="Master volume" VerticalAlignment="Center" FontSize="14">
                </TextBlock>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Slider Grid.Column="0"
                            Name="VolumeSlider"
                            VerticalAlignment="Center"
                            Minimum="0"
                            Maximum="100"
                            TickFrequency="1"
                            SmallChange="1"
                            LargeChange="10"
                            Value="{Binding AudioVolumePercent, Mode=TwoWay}">
                    </Slider>
                    <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"
                               Text="{Binding AudioVolumePercent, StringFormat={}{0:F0}%}" />
                </Grid>
            </Grid>

            <Button Grid.Column="1" Grid.Row="4"
                    Name="SaveButton" Content="Save"
                    Margin="5"
                    FontSize="15"
                    Command="{Binding SaveSettingsCommand}" />
            <Button Grid.Column="2" Grid.Row="4"
                    Name="CloseButton" Content="Close"
                    Margin="5"
                    FontSize="15"
                    Command="{Binding CloseWindowCommand}" />

        </Grid>
    </Border>
</Window>